services:
  app:
    container_name: lumairej_app

    build:
      context: .
      dockerfile: Dockerfile

    env_file:
      - .env

    ports:
      - "8000:8000"

    volumes:
      - ./app:/app/app:delegated
      - pdm_cache:/app/__pypackages__

volumes:
  pdm_cache:
